#Rearrange elements Given an array arr[] of size n, the task is to rearrange it in alternate positive and negative manner without changing the relative order of positive and negative numbers. In case of extra positive/negative numbers, they appear at the end of the array.
def rearrange(l):
    left=0
    right=1
    m=[]
    while right<len(l):
        if l[left]<0:
            pass
    return None